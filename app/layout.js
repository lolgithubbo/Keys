export const metadata = {
  title: "Key Generator",
  description: "Generiert Codes"
};

export default function RootLayout({ children }) {
  return (
    <html lang="de">
      <body>{children}</body>
    </html>
  );
}
